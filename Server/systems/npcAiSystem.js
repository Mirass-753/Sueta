function startNpcAiLoop({ npcs, config }) {
  if (!npcs || !config) {
    return;
  }

  // Placeholder for server-side AI tick. Intentionally empty for now to keep
  // current behavior unchanged until AI logic is implemented.
}

module.exports = {
  startNpcAiLoop,
};
